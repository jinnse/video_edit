# Python 베이스 이미지 사용 (최신 버전)
FROM python:3.9-slim

# 작업 디렉토리 생성
WORKDIR /app

# 필요 라이브러리 설치 (boto3, Flask 등)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 애플리케이션 파일 복사
COPY app.py .

# 로그 설정
RUN mkdir -p /var/log/app
ENV LOG_PATH=/var/log/app/app.log

# 애플리케이션 실행
CMD ["python", "app.py"]