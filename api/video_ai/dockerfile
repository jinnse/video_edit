# Python 베이스 이미지 사용
FROM python:3.9-slim

# 작업 디렉토리 생성
WORKDIR /app

# 시스템 패키지 업데이트 및 필요한 라이브러리 설치 (boto3, flask, dotenv 등)
RUN apt-get update && apt-get install -y \
    gcc \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*

# 필요한 파이썬 라이브러리 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 애플리케이션 코드 복사
COPY app.py .

# Flask 애플리케이션 실행
CMD ["python", "app.py"]
